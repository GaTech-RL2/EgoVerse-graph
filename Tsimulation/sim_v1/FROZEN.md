# FROZEN sim v1

Complete historical tree. Do NOT edit -- it exists to replay
data collected under v1 exactly. New work goes in the repo-root `Tsimulation/`
(current version).

Select it with `get_env("v1")` or `PUSHSHAPES_SIM=v1`.
