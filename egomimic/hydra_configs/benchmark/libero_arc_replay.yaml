# R is accumulated SO(3) rotation in degrees; D is translation arc in metres.
# null removes that cap while retaining the 32-command lookahead.
horizon: 32
execute_steps: 16
dt: 0.05
seed: 20260921
workers: 8
calibration_demos: [0, 1]
selection_demos: [2, 3]
confirmation_demos: [10, 11, 12, 13, 14]
rotation_degrees: [12, 24, 48, 96, null]
translation_metres: [0.05, 0.1, 0.2, 0.4, null]
waypoints: [8, 16, 24, 32, 36]
# Screen only using calibration commands. A short window is never retimed.
minimum_execution_coverage: 0.99
minimum_raw_success: 0.8
minimum_dense_retention: 0.95
# Select the lowest M that loses no raw-successful demonstrations; among ties,
# prefer fewer total failures, then command MSE. Confirmation cannot retune it.
minimum_retention: 1.0
maximum_success_rate_drop: 0.0
