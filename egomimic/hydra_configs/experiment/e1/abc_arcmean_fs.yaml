# @package _global_
# ABC / YAM skirts — arcmean row with FIXED waypoint spacing (h = D/(M-1), plateau past the valid length).
defaults:
- /experiment/e1/abc_arcmean
- _self_
e1:
  fixed_spacing: true
name: e1_abc_arcmean_fs
