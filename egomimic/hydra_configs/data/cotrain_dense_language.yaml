defaults:
  - cotrain_dense_language_base
  - _self_

train_datasets:
  eva_bimanual:
    filters:
      _target_: egomimic.rldb.filters.ScaleAnnotationDatasetFilter
      project_name: dense-language
      filter_lambdas:
        - "lambda row: (row['embodiment'] == 'eva_bimanual') & (row['task'] == 'pick_place') & (row['zarr_processed_path'] != '')"
  human_bimanual:
    filters:
      _target_: egomimic.rldb.filters.ScaleAnnotationDatasetFilter
      project_name: dense-language
      filter_lambdas:
        - "lambda row: (row['embodiment'] == 'human_bimanual') & (row['task'] == 'pick_place') & (row['zarr_processed_path'] != '')"

valid_datasets:
  eva_bimanual:
    filters:
      _target_: egomimic.rldb.filters.ScaleAnnotationDatasetFilter
      project_name: dense-language
      filter_lambdas:
        - "lambda row: row['embodiment'] == 'eva_bimanual' and row['task'] == 'pick_place' and row['zarr_processed_path'] != '' and 'alignment' not in (row.get('task_description') or '')"
  human_bimanual:
    filters:
      _target_: egomimic.rldb.filters.ScaleAnnotationDatasetFilter
      project_name: dense-language
      filter_lambdas:
        - "lambda row: row['embodiment'] == 'human_bimanual' and row['task'] == 'pick_place' and row['zarr_processed_path'] != '' and 'alignment' not in (row.get('task_description') or '')"
