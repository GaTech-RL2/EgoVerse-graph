defaults:
  - default

strategy: ddp

accelerator: gpu
# Lightning expects the device count per node; ``num_nodes`` is separate.
devices: ${launch_params.gpus_per_node}
num_nodes: ${launch_params.nodes}
sync_batchnorm: True
check_val_every_n_epoch: 200
num_sanity_val_steps: 0
