# One shared deterministic episode split for baseline and hybrid experiments.
_target_: egomimic.pl_utils.pl_data_utils.MultiDataModuleWrapper
train_datasets:
  yam_bimanual:
    _target_: egomimic.rldb.zarr.zarr_dataset_multi.MultiDataset._from_resolver
    batch_keys: [observations.state.ee_pose, actions_cartesian]
    resolver:
      _target_: egomimic.rldb.zarr.zarr_dataset_multi.S3EpisodeResolver
      folder_path: ${oc.env:EGOVERSE_ABC_DATASET_DIR}
      image_hw: [480, 640]
      key_map:
        _target_: egomimic.rldb.embodiment.yam.Yam.get_keymap
        keymap_mode: ${abc.action_mode}
        annotation_key: null
        min_distance_unit: ${abc.arc_distance}
        rotation_distance_unit: ${abc.arc_rotation_distance}
      transform_list:
        _target_: egomimic.rldb.embodiment.yam.Yam.get_transform_list
        action_mode: ${abc.action_mode}
        coord_frame: eef_frame
        rotation_mode: euler
        min_distance_unit: ${abc.arc_distance}
        rotation_distance_unit: ${abc.arc_rotation_distance}
        resampled_vector_length: ${abc.arc_waypoints}
        velocity_mode: per_waypoint
    filters:
      _target_: egomimic.rldb.filters.DatasetFilter
      filter_lambdas:
        - "lambda row: row['embodiment'] == 'yam_bimanual' and row['lab'] == 'abc' and (${abc.task_predicate}) and row['zarr_processed_path'] != '' and row['is_deleted'] == False"
    mode: train
    valid_ratio: 0.2
    split_seed: 42
    bounds_check: false
valid_datasets:
  yam_bimanual:
    _target_: egomimic.rldb.zarr.zarr_dataset_multi.MultiDataset._from_resolver
    batch_keys: [observations.state.ee_pose, actions_cartesian]
    resolver: ${data.train_datasets.yam_bimanual.resolver}
    filters: ${data.train_datasets.yam_bimanual.filters}
    mode: valid
    valid_ratio: 0.2
    split_seed: 42
    bounds_check: false
train_dataloader_params:
  yam_bimanual: {batch_size: 32, num_workers: 6}
valid_dataloader_params:
  yam_bimanual: {batch_size: 32, num_workers: 6}
