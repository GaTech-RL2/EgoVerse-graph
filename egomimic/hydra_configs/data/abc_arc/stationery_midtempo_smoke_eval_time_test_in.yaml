# GENERATED by scripts/e1/make_stationery_midtempo_configs.py from the split manifest -- do not edit by hand.
# Stationery mid-tempo holdout (v2-smoke), time row: EVAL-ONLY on 'test_in'; train and valid both point at it, mode=eval never trains
# Pool: ABC 'sort the stationery into containers' + rl2 'organize_stationary', 2247 episodes
# with a Step 0 tau; tertile cuts 0.2991 / 0.3604 m/s. The middle tertile is held out of training.
# train 4 eps / 0.021 h {'abc': 2, 'rl2': 2}; val 2 eps / 0.012 h {'abc': 2}; test_mid 2 eps / 0.013 h {'rl2': 2}; test_in 2 eps / 0.013 h {'abc': 2}.
_target_: egomimic.pl_utils.pl_data_utils.MultiDataModuleWrapper

train_datasets:
  yam_bimanual:
    _target_: egomimic.rldb.zarr.zarr_dataset_multi.MultiDataset._from_resolver
    resolver:
      _target_: egomimic.rldb.zarr.e1_resolvers.S3EpisodeResolverWithEmbodimentOverride
      embodiment_override: yam_bimanual
      folder_path: /storage/project/r-dxu345-0/shared/egoverseS3ZarrDatasets
      image_hw: [480, 640]
      key_map:
        _target_: egomimic.rldb.embodiment.yam.Yam.get_keymap
        keymap_mode: cartesian
      transform_list:
        _target_: egomimic.rldb.embodiment.yam.Yam.get_transform_list
        action_mode: cartesian
        coord_frame: eef_frame
        rotation_mode: euler
    filters:
      _target_: egomimic.rldb.filters.DatasetFilter
      filter_lambdas:
        - "lambda row, S=frozenset({'2b5c12b2-4d10-42c0-b218-a2ba2ba9ca3b','fdf9c1bf-2fa4-4896-b96f-9072cb36063c'}): row['embodiment'] == 'yam_bimanual' and row['lab'] in ('abc', 'rl2') and row['task'] in ('sort the stationery into containers', 'organize_stationary') and row['zarr_processed_path'] != '' and row['is_deleted'] == False and row['episode_hash'] in S"
    mode: total
    valid_ratio: 0.0
    bounds_check: false

valid_datasets:
  yam_bimanual:
    _target_: egomimic.rldb.zarr.zarr_dataset_multi.MultiDataset._from_resolver
    resolver:
      _target_: egomimic.rldb.zarr.e1_resolvers.S3EpisodeResolverWithEmbodimentOverride
      embodiment_override: yam_bimanual
      folder_path: /storage/project/r-dxu345-0/shared/egoverseS3ZarrDatasets
      image_hw: [480, 640]
      key_map:
        _target_: egomimic.rldb.embodiment.yam.Yam.get_keymap
        keymap_mode: cartesian
      transform_list:
        _target_: egomimic.rldb.embodiment.yam.Yam.get_transform_list
        action_mode: cartesian
        coord_frame: eef_frame
        rotation_mode: euler
    filters:
      _target_: egomimic.rldb.filters.DatasetFilter
      filter_lambdas:
        - "lambda row, S=frozenset({'2b5c12b2-4d10-42c0-b218-a2ba2ba9ca3b','fdf9c1bf-2fa4-4896-b96f-9072cb36063c'}): row['embodiment'] == 'yam_bimanual' and row['lab'] in ('abc', 'rl2') and row['task'] in ('sort the stationery into containers', 'organize_stationary') and row['zarr_processed_path'] != '' and row['is_deleted'] == False and row['episode_hash'] in S"
    mode: total
    valid_ratio: 0.0
    bounds_check: false

train_dataloader_params:
  yam_bimanual:
    batch_size: 32
    num_workers: 6

valid_dataloader_params:
  yam_bimanual:
    batch_size: 32
    num_workers: 6
