# @package _global_
defaults:
  - /experiment/pusht/pipeline_sampler_chain_gripper_planar_v2_arc_D80_M32
  - _self_

name: planar_v2_chain_gripper_arc_D80_M32_smoke
description: two_optimizer_steps_plus_scheduled_validation_energy32
trainer:
  max_steps: 2
  limit_train_batches: 2
  val_check_interval: 1
  limit_val_batches: 1
logger: {wandb: {offline: true}}
callbacks:
  model_checkpoint:
    every_n_train_steps: 1
    train_time_interval: null
