# R is accumulated SO(3) rotation in degrees; D is translation arc in metres.
# null removes that cap while retaining the 32-command lookahead.
horizon: 32
execute_steps: 16
dt: 0.05
seed: 20260921
workers: 8
calibration_demos: [0, 1]
selection_demos: [2, 3]
confirmation_demos: [30, 31, 32, 33, 34]
rotation_degrees: [12, 24, 48, 96, 128, 192, 384, null]
translation_metres: [0.05, 0.1, 0.2, 0.4, 0.8, 1.6, null]
waypoints: [4, 8, 16, 24, 32, 36]
# Screen only using calibration commands. A short window is never retimed.
minimum_execution_coverage: 0.99
minimum_raw_success: 0.8
action_dtype: float32
max_dense_action_mse: 1.0e-12
max_raw_repeat_state_error: 0.0
# Contact dynamics amplify even a float64-to-float32 cast. Match aggregate raw
# success, report all paired losses/gains, and never tune on confirmation.
minimum_retention: 0.0
maximum_success_rate_drop: 0.0
