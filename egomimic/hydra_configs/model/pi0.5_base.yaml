_target_: egomimic.pl_utils.pl_model.ModelWrapper
enable_grad_norm: false
optimizer:
  _target_: torch.optim.AdamW
  _partial_: true
  lr: 3.0e-05
  betas:
  - 0.9
  - 0.999
  eps: 1.0e-08
  weight_decay: 0.0
scheduler:
  _target_: transformers.get_cosine_schedule_with_warmup
  _partial_: true
  num_warmup_steps: 2000
  num_training_steps: 60000
  num_cycles: 0.5
scheduler_interval: step
pipeline:
  _target_: egomimic.pipeline.algo.PipelineAlgo
  stage_ids:
    sampler: 0
  stages:
  - _target_: egomimic.pipeline.stages_pi05.PI05Stage
    _recursive_: false
    policy:
      _target_: egomimic.models.pi05.policy.PI
      config:
        pytorch_training_precision: bfloat16
        pytorch_weight_path: ${oc.env:EGOVERSE_PI05_WEIGHTS,/storage/home/hcoda1/4/paphiwetsa3/r-dxu345-0/projects/EgoVerse/egomimic/algo/pi_checkpoints/pi05_base_pytorch}
        model:
          pi05: true
          action_dim: 32
          action_horizon: 100
          max_token_len: 180
      tokenizer_model_name: google/paligemma-3b-mix-224
      tokenizer_max_length: 128
      sampling_mode: random
      annotation_key: annotations
      default_prompt: ''
      proprio_in_prompt: true
      embodiment_label: true
      state_num_bins: 256
      control_mode: null
      train_image_augs:
        _target_: torchvision.transforms.Compose
        transforms:
        - _target_: torchvision.transforms.Resize
          size: 224
          interpolation: 3
      eval_image_augs:
        _target_: torchvision.transforms.Compose
        transforms:
        - _target_: torchvision.transforms.Resize
          size: 224
          interpolation: 3
inference:
  status: unsupported
  reason: 'PI base is a configuration fragment: choose a concrete pi0.5_bc_* or pi0.5_cotrain_*
    recipe with source/action/camera declarations.'
