# GENERATED by scripts/e1/make_stationery_midtempo_configs.py from the split manifest -- do not edit by hand.
# Stationery mid-tempo holdout (v2-smoke), arcdur row: training on train, in-training validation on val
# Pool: ABC 'sort the stationery into containers' + rl2 'organize_stationary', 2247 episodes
# with a Step 0 tau; tertile cuts 0.2991 / 0.3604 m/s. The middle tertile is held out of training.
# train 4 eps / 0.021 h {'abc': 2, 'rl2': 2}; val 2 eps / 0.012 h {'abc': 2}; test_mid 2 eps / 0.013 h {'rl2': 2}; test_in 2 eps / 0.013 h {'abc': 2}.
_target_: egomimic.pl_utils.pl_data_utils.MultiDataModuleWrapper

train_datasets:
  yam_bimanual:
    _target_: egomimic.rldb.zarr.zarr_dataset_multi.MultiDataset._from_resolver
    resolver:
      _target_: egomimic.rldb.zarr.e1_resolvers.S3EpisodeResolverWithEmbodimentOverride
      embodiment_override: yam_bimanual
      folder_path: /storage/project/r-dxu345-0/shared/egoverseS3ZarrDatasets
      image_hw: [480, 640]
      key_map:
        _target_: egomimic.rldb.embodiment.bimanual_arc.get_keymap
        horizon: 200
        embodiment: yam
        drop_wrist_images: false
      transform_list:
        _target_: egomimic.rldb.embodiment.bimanual_arc.get_transform_list
        variant: arcdur
        chunk_length: 200
        time_rows: 100
        min_distance_unit: 0.40
        resampled_vector_length: 100
        stride: 1
        rotation_mode: euler
        velocity_norm: path
        embodiment: yam
    filters:
      _target_: egomimic.rldb.filters.DatasetFilter
      filter_lambdas:
        - "lambda row, S=frozenset({'1887e7f5-0422-4957-adf3-9afd33a63b8d','2026-09-15-05-48-13-511000','2026-09-15-05-50-19-823000','b98844e3-76b0-4aad-a597-497e6a12165f'}): row['embodiment'] == 'yam_bimanual' and row['lab'] in ('abc', 'rl2') and row['task'] in ('sort the stationery into containers', 'organize_stationary') and row['zarr_processed_path'] != '' and row['is_deleted'] == False and row['episode_hash'] in S"
    mode: total
    valid_ratio: 0.0
    bounds_check: false

valid_datasets:
  yam_bimanual:
    _target_: egomimic.rldb.zarr.zarr_dataset_multi.MultiDataset._from_resolver
    resolver:
      _target_: egomimic.rldb.zarr.e1_resolvers.S3EpisodeResolverWithEmbodimentOverride
      embodiment_override: yam_bimanual
      folder_path: /storage/project/r-dxu345-0/shared/egoverseS3ZarrDatasets
      image_hw: [480, 640]
      key_map:
        _target_: egomimic.rldb.embodiment.bimanual_arc.get_keymap
        horizon: 200
        embodiment: yam
        drop_wrist_images: false
      transform_list:
        _target_: egomimic.rldb.embodiment.bimanual_arc.get_transform_list
        variant: arcdur
        chunk_length: 200
        time_rows: 100
        min_distance_unit: 0.40
        resampled_vector_length: 100
        stride: 1
        rotation_mode: euler
        velocity_norm: path
        embodiment: yam
    filters:
      _target_: egomimic.rldb.filters.DatasetFilter
      filter_lambdas:
        - "lambda row, S=frozenset({'80acd04d-1de8-42f4-9119-2bae30e1d351','d29b11ef-4083-439d-ab80-198af656c75e'}): row['embodiment'] == 'yam_bimanual' and row['lab'] in ('abc', 'rl2') and row['task'] in ('sort the stationery into containers', 'organize_stationary') and row['zarr_processed_path'] != '' and row['is_deleted'] == False and row['episode_hash'] in S"
    mode: total
    valid_ratio: 0.0
    bounds_check: false

train_dataloader_params:
  yam_bimanual:
    batch_size: 32
    num_workers: 6

valid_dataloader_params:
  yam_bimanual:
    batch_size: 32
    num_workers: 6
