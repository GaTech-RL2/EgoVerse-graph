# @package _global_
# ABC / YAM skirts — arcdur row: Ryan's duration codec (see fold_arcdur).
defaults:
- /experiment/e1/abc_base
- override /model: e1/hpt_flow
- _self_
e1:
  variant: arcdur
  chunk_length: 200
  action_dim: 16
  action_horizon: 100
